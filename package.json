{
  "name": "gameplan",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "lint": "npm run lint --workspace=web",
    "start": "npm run start --workspace=web",
    "ingest": "npm run ingest --workspace=web",
    "ingest:all": "bash scripts/ingest_all.sh"
  }
}
